/** @type {import('next').NextConfig} */
const nextConfig = {
  // PWA設定準備: next-pwa等を導入する際にここに追加
  // const withPWA = require('next-pwa')({ dest: 'public' });
  // module.exports = withPWA(nextConfig);
};

module.exports = nextConfig;
